<!-- Barra Superior (Navbar) -->
<%= render 'pos/main/nav' %>

<!-- Contenedor Principal -->
<div class="flex flex-1" data-controller="layout">
  <!-- Columna Izquierda -->
  <%= render 'pos/main/left_column' %>

  <!-- Columna Derecha -->
  <%= render 'pos/main/right_column' %>
</div>

<% if params[:print_order_id].present? %>
  <iframe src="<%= receipt_preview_order_path(params[:print_order_id]) %>" style="display:none;"></iframe>
<% end %>